Parabéns ao baixar um CRUD a ser incrementado!!
Isso pode ser considerado como um protótipo de uma aplicação a qual tem, já implementado, um gerenciamento de usuários básico(o qual também pode ser incrementado)
Caso você seja da BIT, o objetivo dessa aplicação é aprender sobre nodejs e javascript enquanto você adiciona novas features na aplicação

Variáveis de ambiente necessárias:
PORT, ou seja, a porta que a aplicação inicializará
DATABASE_URL, ou seja, url do banco de dados MONGODB que se deseja conectar (caso usar local, utilize DATABASE_URL=mongodb://localhost:27017/nome_do_banco)
SESSION_SECRET, o segredo para geração de cookies da seção de login dos usuários

Para rodar a aplicação:
Instale as dependencias com: npm install (apenas 1 vez)
Inicie o servidor com: node server.js

GLHF!!